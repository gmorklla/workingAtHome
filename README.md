# Get started

### Inicio 

Una vez clonado se ejecuta en la consola: `npm install`
   
### Next

banorte

Verificar que el puerto coincida con el port del backend en el archivo `projects/banorte/src/app/shared/data/port.ts`

banorteRender

Verificar que el puerto coincida con el port del backend en el archivo `projects/banorte-render/src/app/shared/data/port.ts`

### Next

Para correr la app que crea los controles, camáñas, ventanas y demás ejecutar:

`ng serve banorte`

Para correr la app que renderiza ejecutar:

`ng serve banorteRender`

