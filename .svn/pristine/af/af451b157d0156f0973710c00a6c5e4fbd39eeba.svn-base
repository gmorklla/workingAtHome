import { TestBed } from '@angular/core/testing';

import { WindowEditService } from './window-edit.service';

describe('WindowEditService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: WindowEditService = TestBed.get(WindowEditService);
    expect(service).toBeTruthy();
  });
});
