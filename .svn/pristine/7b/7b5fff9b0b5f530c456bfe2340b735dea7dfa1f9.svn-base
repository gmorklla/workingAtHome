import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ThresholdColorComponent } from './threshold-color.component';

describe('ThresholdColorComponent', () => {
  let component: ThresholdColorComponent;
  let fixture: ComponentFixture<ThresholdColorComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ThresholdColorComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ThresholdColorComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
