import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RenderIframeComponent } from './render-iframe.component';

describe('RenderIframeComponent', () => {
  let component: RenderIframeComponent;
  let fixture: ComponentFixture<RenderIframeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RenderIframeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RenderIframeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
