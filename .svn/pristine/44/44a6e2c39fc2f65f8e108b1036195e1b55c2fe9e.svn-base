import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ContainerStylesWinComponent } from './container-styles-win.component';

describe('ContainerStylesWinComponent', () => {
  let component: ContainerStylesWinComponent;
  let fixture: ComponentFixture<ContainerStylesWinComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ContainerStylesWinComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ContainerStylesWinComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
