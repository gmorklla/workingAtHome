import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RuleVariableDialogComponent } from './rule-variable-dialog.component';

describe('RuleVariableDialogComponent', () => {
  let component: RuleVariableDialogComponent;
  let fixture: ComponentFixture<RuleVariableDialogComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RuleVariableDialogComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RuleVariableDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
