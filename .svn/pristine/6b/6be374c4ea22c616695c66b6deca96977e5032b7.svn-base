import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RenderInputsComponent } from './render-inputs.component';

describe('RenderInputsComponent', () => {
  let component: RenderInputsComponent;
  let fixture: ComponentFixture<RenderInputsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RenderInputsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RenderInputsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
