import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SocketCommComponent } from './socket-comm.component';

describe('SocketCommComponent', () => {
  let component: SocketCommComponent;
  let fixture: ComponentFixture<SocketCommComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SocketCommComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SocketCommComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
