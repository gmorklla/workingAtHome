import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RuleVariableComponent } from './rule-variable.component';

describe('RuleVariableComponent', () => {
  let component: RuleVariableComponent;
  let fixture: ComponentFixture<RuleVariableComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RuleVariableComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RuleVariableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
