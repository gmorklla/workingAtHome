import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RenderAComponent } from './render-a.component';

describe('RenderAComponent', () => {
  let component: RenderAComponent;
  let fixture: ComponentFixture<RenderAComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RenderAComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RenderAComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
