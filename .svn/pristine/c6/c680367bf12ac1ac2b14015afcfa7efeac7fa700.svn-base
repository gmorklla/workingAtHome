import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormDivMessageComponent } from './form-div-message.component';

describe('FormDivMessageComponent', () => {
  let component: FormDivMessageComponent;
  let fixture: ComponentFixture<FormDivMessageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FormDivMessageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FormDivMessageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
