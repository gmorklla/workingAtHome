import { RuleControlModule } from './rule-control.module';

describe('RuleControlModule', () => {
  let ruleControlModule: RuleControlModule;

  beforeEach(() => {
    ruleControlModule = new RuleControlModule();
  });

  it('should create an instance', () => {
    expect(ruleControlModule).toBeTruthy();
  });
});
