import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DivMessageComponent } from './div-message.component';

describe('DivMessageComponent', () => {
  let component: DivMessageComponent;
  let fixture: ComponentFixture<DivMessageComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DivMessageComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DivMessageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
