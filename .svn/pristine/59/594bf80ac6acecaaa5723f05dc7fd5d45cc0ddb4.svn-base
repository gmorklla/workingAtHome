import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormSelectLibComponent } from './form-select-lib.component';

describe('FormSelectLibComponent', () => {
  let component: FormSelectLibComponent;
  let fixture: ComponentFixture<FormSelectLibComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FormSelectLibComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FormSelectLibComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
